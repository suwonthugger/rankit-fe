{
  "name": "rankit",
  "private": true,
  "packageManager": "pnpm@9.2.0",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "test": "turbo run test"
  },
  "tasks": {
    "build": {
      "outputs": [
        "dist/**"
      ]
    }
  },
  "devDependencies": {
    "turbo": "latest"
  },
  "dependencies": {
    "@radix-ui/react-alert-dialog": "^1.1.2"
  }
}
